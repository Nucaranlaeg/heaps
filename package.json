{
  "scripts": {
    "sass": "sass --watch src/script/ dist/script/",
    "ts": "tsc --watch",
    "watch": "npm run sass && npm run ts"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "sass": "^1.62.1"
  }
}
